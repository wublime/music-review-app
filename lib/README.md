# Lib structure (Epic 1 — M1.1)

- **models/** — Data classes (e.g. `MusicRelease`) and DTOs.
- **widgets/** — Reusable UI components (e.g. `ReleaseCard`).
- **screens/** — High-level dashboard and app views.
- **services/** — Mock data loading, timer logic, and future API clients.
